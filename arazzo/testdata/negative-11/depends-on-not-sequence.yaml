# step-level dependsOn is a sequence of step identifiers, not a mapping.
arazzo: 1.1.0
info:
  title: negative dependsOn
  version: 1.0.0
workflows:
  - workflowId: wf
    steps:
      - stepId: s1
        operationId: op
        dependsOn:
          not: a-sequence
